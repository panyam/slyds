# Nothing generated lives here anymore. buf.yaml, buf.gen.yaml, and buf.lock
# are committed: the lock file is what pins the buf.build/mcpkit/protogen
# annotations by digest, so a regen is reproducible.
